#include "colors.h"
#include "common.h"
// Custom print function
void printError(char str[]);
void printErrorReason(char str[]);
void printSuccess(char str[]);
void printHorizontalLine(char designChar);
void printHighlight(char str[]);